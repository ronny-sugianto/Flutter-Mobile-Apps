import 'package:flutter/material.dart';

class GeneralNotFoundScreen extends StatelessWidget {
  const GeneralNotFoundScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
